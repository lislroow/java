<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD mapper 3.0//EN" 
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="spring.auth.api.dao.UserLoginDao">
  
  <select id="selectMemberByLoginId" resultType="MemberLoginVo">
    SELECT /* UserLoginDao.selectMemberByLoginId */
           id,
           login_id,
           login_pwd,
           roles
    FROM   au_member A
    WHERE  A.login_id = #{loginId}
  </select>
  
  <select id="selectManagerByLoginId" resultType="ManagerLoginVo">
    SELECT /* UserLoginDao.selectManagerByLoginId */
           id,
           login_id,
           login_pwd,
           roles,
           mgr_name,
           enable_yn,
           locked_yn,
           pwd_exp_date
    FROM   au_manager A
    <where>
       AND A.login_id = #{loginId}
    </where>
  </select>
  
</mapper>
