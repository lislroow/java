<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD mapper 3.0//EN" 
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="spring.auth.api.dao.TokenDao">
  
  <select id="findByTokenId" resultType="TokenVo">
    SELECT /* TokenDao.findByTokenId */
           token_id, token, client_id, enable_yn, locked_yn
    FROM   au_client_token
    WHERE  token_id = #{tokenId}
  </select>
  
  <insert id="insert">
    INSERT /* TokenDao.insert */
      INTO au_client_token (
      token_id,
      user_id,
      token
    ) VALUES (
      #{tokenId},
      #{userId},
      #{token}
    )
  </insert>
  
</mapper>
