<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD mapper 3.0//EN" 
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="spring.auth.common.login.dao.TokenDao">
  
  <select id="findClientTokenByTokenKey">
    SELECT /* TokenDao.findClientTokenByTokenKey */
           A.client_id
           , A.token_key
           , A.token_value
           , A.enable_yn
           , A.client_ip
           , A.exp_date
    FROM   au_client A
      LEFT JOIN au_client_token B ON A.client_id = B.id
    <where>
       AND A.token_key = #{tokenKey}
    </where>
  </select>
  
</mapper>
