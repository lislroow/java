<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD mapper 3.0//EN" 
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="spring.auth.api.dao.MemberAuthDao">
  
  <select id="selectByLoginId" resultType="MemberAuthVo">
    SELECT /* MemberAuthDao.selectByLoginId */
           id,
           login_id,
           login_pwd,
           roles
    FROM   au_member A
    WHERE  A.login_id = #{loginId}
    UNION ALL
    SELECT id,
           login_id,
           login_pwd,
           roles
    FROM   au_manager A
    WHERE  A.login_id = #{loginId}
  </select>
  
  <insert id="insert">
    INSERT /* MemberAuthDao.insert */
      INTO au_member (
      id,
      registration_id,
      oauth2_id,
      login_id,
      nickname,
      create_id,
      modify_id
    ) VALUES (
      #{id},
      #{registrationId},
      #{oauth2Id},
      #{loginId},
      #{nickname},
      #{id},
      #{id}
    )
  </insert>
  
</mapper>
