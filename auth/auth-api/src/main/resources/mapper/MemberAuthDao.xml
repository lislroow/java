<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD mapper 3.0//EN" 
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="spring.auth.api.dao.MemberAuthDao">
  
  <select id="selectByEmail" resultType="MemberAuthVo">
    SELECT /* MemberAuthDao.selectByEmail */
           '{bcrypt}$2a$10$N3gf.ZvWGhen86NCWDdnmenUQqLBmAN1rQWeMJQHMr.sz3SF9..EW' as password,
           'ROLE_MEMBER' as role,
           'Y' as memberYn,
           registration_id,
           oauth2_id,
           email,
           nickname
    FROM   member a
    WHERE  a.email = #{email}
    UNION ALL
    SELECT '{bcrypt}$2a$10$N3gf.ZvWGhen86NCWDdnmenUQqLBmAN1rQWeMJQHMr.sz3SF9..EW' as password,
           'ROLE_MANAGER' as role,
           'N' as memberYn,
           '' as registration_id,
           '' as oauth2_id,
           mgr_id as email,
           mgr_name as nickname
    FROM   manager a
    WHERE  a.mgr_id = #{email}
  </select>
  
  <insert id="insert">
    INSERT /* MemberAuthDao.insert */
      INTO member (
      registration_id,
      oauth2_id,
      email,
      nickname
    ) VALUES (
      #{registrationId},
      #{oauth2Id},
      #{email},
      #{nickname}
    )
  </insert>
  
</mapper>
