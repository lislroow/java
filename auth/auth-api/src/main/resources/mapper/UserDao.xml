<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD mapper 3.0//EN" 
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="spring.auth.common.login.dao.UserDao">
  
  <select id="selectMemberInfoById" resultType="MemberVo">
    SELECT /* UserDao.selectMemberInfoById */
           id,
           roles,
           login_id,
           nickname
    FROM   au_member a
    WHERE  a.id = #{id}
  </select>
  
  <select id="selectManagerInfoById" resultType="ManagerVo">
    SELECT /* UserDao.selectManagerInfoById */
           id,
           roles,
           login_id,
           mgr_name
    FROM   au_manager a
    WHERE  a.id = #{id}
  </select>
  
</mapper>
