<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD mapper 3.0//EN" 
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="spring.auth.api.dao.UserDao">
  
  <select id="selectMemberById" resultType="MemberVo">
    SELECT /* UserDao.selectMemberById */
           id,
           roles,
           login_id,
           nickname
    FROM   au_member a
    WHERE  a.id = #{id}
  </select>
  
  <select id="selectManagerById" resultType="ManagerVo">
    SELECT /* UserDao.selectManagerById */
           id,
           roles,
           login_id,
           mgr_name
    FROM   au_manager a
    WHERE  a.id = #{id}
  </select>
  
</mapper>
