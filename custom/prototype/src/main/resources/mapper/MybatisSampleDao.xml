<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD mapper 3.0//EN" 
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="spring.custom.api.dao.MybatisSampleDao">
  
  <select id="findAll" resultType="ScientistVo">
    SELECT /* MybatisCrudDao.findAll */
           *
    FROM   scientist
    ORDER BY id DESC
  </select>
  
  <select id="findById" resultType="ScientistVo">
    SELECT /* MybatisCrudDao.findById */
           *
    FROM   scientist
    WHERE  id = #{id}
  </select>
  
  <select id="findList">
    SELECT /* MybatisCrudDao.findList */
           *
    FROM   scientist
    ORDER BY id DESC
  </select>
  
  <select id="findListByName" resultType="ScientistVo">
    SELECT /* MybatisCrudDao.findListByName */
           *
    FROM   scientist
    <if test="vo.name != null and !''.equals(vo.name)">
      WHERE UPPER(name) LIKE '%' || UPPER(#{vo.name}) || '%'
    </if>
    ORDER BY id DESC
  </select>
  
  <insert id="add">
    INSERT /* MybatisCrudDao.add */
      INTO scientist (
        name,
        birth_year,
        death_year,
        create_date,
        modify_date,
        create_id,
        modify_id
      ) VALUES (
        #{name},
        #{birthYear},
        #{deathYear},
        now(),
        now(),
        #{createId},
        #{modifyId}
      )
  </insert>
  
  <update id="modifyNameById">
    UPDATE /* MybatisCrudDao.modifyNameById */
           scientist
       SET name = #{name},
           birth_year = #{birthYear},
           death_year = #{deathYear},
           modify_date = now(),
           modify_id = #{modifyId}
     WHERE id = #{id}
  </update>
  
  <delete id="removeById">
    DELETE /* MybatisCrudDao.removeById */
      FROM scientist
     WHERE id = #{id}
  </delete>
  
</mapper>
